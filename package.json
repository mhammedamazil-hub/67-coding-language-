{
  "name": "lang67",
  "version": "1.0.0",
  "description": "67 — a programming language whose source alphabet contains only the symbols 6 (binary 0) and 7 (binary 1).",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:examples": "tsx scripts/generate-examples.ts",
    "prebuild": "npm run gen:examples",
    "pretest": "npm run gen:examples"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
